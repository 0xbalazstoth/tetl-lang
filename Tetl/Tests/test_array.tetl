>> Array tests

array = [1, 2, 3];
OutNewLine(array.Length());

a = 5;
array.Add(10-1);
array.Add(a * 20);
array.Add(20);

i = 0;
while (i < array.Length())
{
    OutNewLine(array[i]);
    i = i + 1;
}

array.Remove(9);
i = 0;
while (i < array.Length())
{
    OutNewLine(array[i]);
    i = i + 1;
}