>> Array tests

array = [43, 2, 3];

a = 5;
array.Add(a * -10);

i = 0;
while (i < array.Length())
{
    OutNewLine(array[i]);
    i = i + 1;
}

array.Remove(-50);
i = 0;
while (i < array.Length())
{
    OutNewLine(array[i]);
    i = i + 1;
}