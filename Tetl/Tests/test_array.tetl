>> Array tests

>> Update
OutNewLine(">> Update");

array = ['a', 'b', 'c'];
update = true;
array[2 - 1 + 1] = 1 + 5 * 3 % 2;
array[0] = update;
foreach (elem in array)
{
    currentElement = elem;
    OutNewLine(currentElement);
}

>> Add
OutNewLine(">> Add");

a = 5;
array.Add(a * -10);

i = 0;
while (i < array.Length())
{
    OutNewLine(array[i]);
    i = i + 1;
}

>> Remove
OutNewLine(">> Remove");

array.Remove(-50);
i = 0;
while (i < array.Length())
{
    OutNewLine(array[i]);
    i = i + 1;
}

>> Reverse
OutNewLine(">> Reverse");
array = ["test", "blabla", "haha"];
foreach (elem in array)
{
    OutNewLine(elem);
}

array.Reverse();
OutNewLine(">> Reversed");

foreach (elem in array)
{
    OutNewLine(elem);
}